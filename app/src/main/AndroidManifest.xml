<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED: Live camera observation of physical skill task -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- REQUIRED: Haptic vibration feedback on correct/error/completion events -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Camera hardware features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!--
      PRIVACY & LEAST PRIVILEGE:
      - No INTERNET permission (100% offline on-device processing)
      - No RECORD_AUDIO (microphone not used in MVP)
      - No storage permissions (all inference in-memory, session DB local)
      - No MANAGE_THERMAL (system-only permission removed; uses standard PowerManager)
      - No unused foreground service permissions
    -->

    <application
        android:name=".SkillLensApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkillLens"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SkillLens.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
